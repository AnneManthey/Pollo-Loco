class Collectables extends MovableObject {

    /**
     * Base constructor for collectable objects.
     * @param {number} x Horizontal world position.
     * @param {number} y Vertical world position.
     */
    constructor (x,y){
        super();
        this.x = x;
        this.y = y;
    }

}